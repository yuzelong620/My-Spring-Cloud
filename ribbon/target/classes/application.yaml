server:
  port: 9090 #当前 Ribbon 服务端口。
spring:
  application:
    name: ribbon  #当前服务注册在 Eureka Server 上的名称。
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/  #注册中心的访问地址。
  instance:
    prefer-ip-address: true   #是否将当前服务的 IP 注册到 Eureka Server。